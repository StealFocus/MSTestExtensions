<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- 
    IMPORTANT - please do not customise this .targets file. Modifications will affect all Solutions using the software 
    factory and maybe overridden by future updates to the software factory. Customise your build process by editing 
    the *.targets/*.proj files in your solution.
  -->

  <!--<ItemGroup>
    <XmlFileCheckerFilesToCheck Include="$(SolutionDir)*\*.csproj" />
  </ItemGroup>

  <PropertyGroup Condition=" '$(SilverlightApplication)'!='true' ">
    <XmlFileCheckerConfigurationFilePath>$(SolutionDir)Scripts\XmlFileCheckerConfiguration-AssemblyProjectFile-CSharp.xml</XmlFileCheckerConfigurationFilePath>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(SilverlightApplication)'=='true' ">
    <XmlFileCheckerConfigurationFilePath>$(SolutionDir)Scripts\XmlFileCheckerConfiguration-SilverlightProjectFile-CSharp.xml</XmlFileCheckerConfigurationFilePath>
  </PropertyGroup>-->

</Project>